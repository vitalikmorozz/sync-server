# Server Configuration
PORT=3006
NODE_ENV=development
LOG_LEVEL=debug

# Database Configuration
DATABASE_URL=postgresql://syncuser:syncpass@localhost:5432/syncserver
POSTGRES_USER=syncuser
POSTGRES_PASSWORD=syncpass
POSTGRES_DB=syncserver
POSTGRES_PORT=5432

# Security
# Generate a secure admin key: node -e "console.log('sk_admin_' + require('crypto').randomBytes(24).toString('base64url'))"
ADMIN_API_KEY=sk_admin_CHANGE_ME_IN_PRODUCTION

# CORS (comma-separated origins, or * for all)
CORS_ORIGINS=*
